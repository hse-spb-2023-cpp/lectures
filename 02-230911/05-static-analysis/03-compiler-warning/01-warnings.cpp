#include <iostream>

int main() {
    int a, b;
    std::cin >> a >> a;
    // Possible to suppress, but very specific for each compiler.
    // Should not happen in this term.
    std::cout << a + b;
}
